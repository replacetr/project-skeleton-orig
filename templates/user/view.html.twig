{% extends 'base.html.twig' %}

{% block title %}View  {% endblock %}

 {% block body %}
<div class="container">
  <div class="row justify-content-start">
    <div class="col-2 border">
    <br>
   {% set o = [1,2,3,4,5]%}
   {% for i in o %}  
      <ul class="border">
      <li>
      <a href="#">Hello</a>
      </li>
      </ul>
      {% endfor %}
   
    </div>
    <div class="col-4">
       <img src="{{ asset('/images/' ~ product.productImage) }}">
         <h3>{{product.productName}} </h3>
       <h6> Price RM :{{product.productPrice|number_format(2, '.', ',' )}}</h6>
        <p>Descriptrion : {{product.description}}</p>
       <h6> <p>Stock Left : {{product.productStock}}</p></h6>
 <a class="btn btn-primary" href="/cart/{{product.id}}" class="">Add To Cart</a>

    </div>
  
  </div>
</div>



{% endblock %}

